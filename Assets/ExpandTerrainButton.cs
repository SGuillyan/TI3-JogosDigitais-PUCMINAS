using UnityEngine;

public class ExpandTerrainButton : MonoBehaviour
{
    public TilemapManager tilemapManager;

    public void ExpandTerrain()
    {
        /*// Define a posição inicial e o tamanho do terreno a ser expandido
        Vector3Int startPosition = new Vector3Int(10, 10, 0);  // Altere conforme necessário
        Vector2Int size = new Vector2Int(5, 5);  // Expande uma área de 5x5 tiles

        tilemapManager.ExpandTerrain(startPosition, size);*/
    }
}
